exec does not create a new process, rather, it dumps the new program by re-initializing the address space of the current process

Round-Robin (RR) scheduling: instead of running jobs to completion, RR runs a job for a time slice (sometimes called a scheduling quantum) and then switches to the next job in the run queue

evaluate performance: turnaround time, response time

incorporating I/O, overlaps allows better use of resources
